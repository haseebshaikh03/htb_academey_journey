# Module 54 — Attacking Web Applications With Ffuf
## Section 4: Page Fuzzing

## Approach
Fuzzed for page names inside the directory discovered in Section 3 (`/forum/FUZZ`), using a common-passwords-themed wordlist as the section teaches. The matching page returned the flag.

Flag found by fuzzing page names with a passwords-themed wordlist against the discovered directory.
