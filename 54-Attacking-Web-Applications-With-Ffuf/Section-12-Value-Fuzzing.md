# Module 54 — Attacking Web Applications With Ffuf
## Section 12: Value Fuzzing

Once the `user` param (from Section 10) was known, fuzzed its **value** (e.g. numeric IDs / usernames) with ffuf to find the value that returns the flag.

**Flag:** `HTB{p4r4m373r_fuzz1n6_15_k3y!}`
