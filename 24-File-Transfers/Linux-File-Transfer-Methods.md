# Module 24 — File Transfers
## Section: Linux File Transfer Methods

**Q1 — `flag.txt` content — Approach:** retrieved via standard Linux file-transfer methods taught in the section theory (e.g. `scp`/`curl`/`nc`/base64-over-terminal).

**Q2 — `hasher` output hash — Approach:** transferred the target file to the box using the same methods, then ran the target's `hasher` utility on it to produce the answer.
